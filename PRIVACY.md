# 隐私说明

SekerEagle 是自托管软件。默认代码不包含遥测、广告 SDK 或第三方分析服务，也不会把素材
发送给云端模型。操作者控制 PostgreSQL、MinIO、日志、备份和模型缓存。

## 处理的数据

- 上传或导入的图片、视频与派生预览。
- 文件名、标签、文件夹、颜色、尺寸、时间等素材元数据。
- 浏览器采集时的页面来源、经过清理的资源 URL 和用户提供的初始标签。
- 本机生成的 embedding、标签原型、建议与审核记录。
- 账号、会话、哈希后的 PAT 和必要的安全审计状态。

浏览器插件不会把 SekerEagle PAT 发送给被采集站点；对象上传使用独立的签名 URL。页面
来源和资源 URL 仍可能包含敏感语义，用户应在采集前判断是否适合保存。

## 操作者责任

公开部署的操作者是其用户数据的管理者，必须根据适用法律提供隐私告知、访问/导出/删除
机制、保留期限、备份策略和事件响应。删除数据库记录前还应确认对象存储派生文件和备份
的保留行为。

如果分发浏览器扩展，应在商店页面链接本说明，并如实声明 host permissions、远端 endpoint
和数据用途。任何未来遥测功能必须默认关闭、显式征得同意并更新本文件。
